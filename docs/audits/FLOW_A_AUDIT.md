FLOW A — Viewer khám phá (rà lại 2026-08-21)

Mốc rà: commit `a08fe2c` + working tree CHƯA COMMIT (19 file M, 22 file/thư mục ??
của đợt 8 — badges, invites, rls, dev/switch, full-tree, share-tree).
Cách đo: dựng dev server RIÊNG `NEXT_DIST_DIR=.next-audit PORT=3100` (Node 20) để
không đụng server cổng 3000 của người dùng — server 3000 đang hỏng chunk cache
(`Cannot find module './vendor-chunks/@tanstack+query-core@5.100.9.js'` → mọi
`/share/*` trả 500; đây là lỗi cache `.next`, KHÔNG phải lỗi code).
Ẩn danh thật sự đo bằng cách chạy server với `DEV_AUTH_BYPASS_USER_ID=" "` (rỗng
sau trim → `getDevBypassUser()` trả null). Người lạ-đã-đăng-nhập đo bằng cookie
`dev_bypass_user=99999999-9999-9999-9999-999999999999`.
LƯU Ý: trong lúc rà, DB `competency` bị một phiên khác ghi đồng thời (0 → 5 dòng
`node_resources`, 0 → 2 nhóm `user_node_progress`, một workspace "AUDIT E FORK
TEST" xuất hiện rồi biến mất). Mọi con số dưới đây kèm thời điểm đo.

Bước: 8 ĐỦ · 0 THIẾU · 0 ĐỨT · 6 SAI
---
A1 | ĐỦ | application/src/app/discover/page.tsx:52-61 · application/src/components/discover/discover-grid.tsx:143-208 | `curl :3100/discover` → 200. Danh sách chỉ chứa `href="/share/devops-test"` + `href="/share/sample-public-roadmap"`; `grep -c "0000-7n1w"` (slug workspace private) = **0**. `psql`: 3 workspace, 2 public-readonly + 1 private → khớp. Có search + lọc loại + 3 kiểu sắp xếp + empty-state 2 nhánh. | Không cần vá.
A2 | SAI | application/src/app/share/[slug]/page.tsx:79 và :194-199 | Tên ĐÚNG (`<title>DevOps Mastery 2026 · Roadmap`). Mô tả thì KHÔNG: subtitle/`og:description` chỉ lấy được description khi workspace có **đúng 1 root**. `psql`: cả hai workspace public đều có **2 root** (`select count(*) filter (where parent_id is null)` = 2) → trang render câu placeholder "Lộ trình học tập — chế độ chia sẻ chỉ xem…" và `<meta property="og:description" content="Lộ trình học tập — 166 mục"/>`. `\d workspaces` xác nhận **không có cột description**. | Sửa `share/[slug]/page.tsx:194-199`: khi `heroRoot === null` lấy `fullTree.find(r => r.description)?.description` làm subtitle, và cùng logic ở `generateMetadata` dòng 68-79 (bỏ điều kiện `rootRow.length === 1`). Vá triệt để hơn: thêm cột `workspaces.description` + migration + ô nhập ở settings, rồi mọi bề mặt public đọc từ đó.
A3 | ĐỦ | application/src/lib/tree/full-tree.ts:30-83 · application/src/components/share/share-tree.tsx:53-119 | `grep -o '/share/devops-test/n/[a-z0-9-]*' \| sort -u \| wc -l` trên HTML trả về = **166**, đúng bằng `select count(*) from roadmap_tree_nodes` = 166, `max(depth)` = 4. Cả 4 cấp nằm trên MỘT trang, không phải click từng node. Bản rà cũ ghi "chỉ render 2 cấp" → đã vá. | Không cần vá phần độ sâu (xem A3b cho phần số đếm).
A3b | SAI | application/src/lib/tree/full-tree.ts:45 và :73-80 | `getFullTree` chỉ `orderBy(asc(orderIndex))` rồi cộng dồn descendantCount bằng cách duyệt NGƯỢC, giả định "con luôn đứng sau cha". Giả định sai: `psql` cho thấy depth 1 có order_index 0..4 còn depth 4 có order_index 0..1 → sắp xếp toàn cục theo order_index KHÔNG bảo đảm thứ tự cha-con. Hậu quả đo được: hàng gốc render "**48 mục**" trong khi truy vấn đệ quy cho **159**; "Giai đoạn 1: Foundational Track" render "**15 mục**" trong khi thật là **46**. Kéo theo `defaultCollapsed` tính total = 55 < `EXPAND_ALL_LIMIT` 60 nên cây bung hết — A3 đang ĐỦ một cách ăn may. | Sửa `full-tree.ts:45` thành `.orderBy(asc(roadmapTreeNodes.depth), asc(roadmapTreeNodes.orderIndex))` (khi đó duyệt ngược mới thật sự là children-first), hoặc bỏ hẳn giả định thứ tự: đệ quy `countSubtree(node)` sau khi dựng xong `children`. **Chú ý tác dụng phụ**: sửa xong total = 166 ≥ 60 → `defaultCollapsed` sẽ thu gọn depth ≥ 2 và A3 mất tính "thấy tất cả level". Phải nâng `EXPAND_ALL_LIMIT` hoặc đổi heuristic sang "thu gọn từ depth ≥ 3" cùng lúc. Nên kèm 1 unit test cho `getFullTree` (hiện `tests/unit/` không có file nào cho full-tree/share-tree).
A4 | SAI | application/src/app/share/[slug]/page.tsx:151-158, :167-171, :239 · application/src/lib/tree/completion.ts:24-27 | Số CÓ chạy ra thật: chip thứ 3 hiển thị "166 / TỔNG MỤC / **84% người hoàn thành**"; `psql` cùng lúc: owner `77b020ab…` có 140/166 done = 84% → khớp. Hai lệch: (1) **nhãn sai bản chất** — `averageCompletionPct` là % hoàn thành TRUNG BÌNH của những người CÓ tiến độ (ở đây n = 1 người), không phải "tỉ lệ người hoàn thành" như chữ đang viết; (2) **mobile cắt mất số** — đo Playwright ở viewport 360px: div `.truncate` chứa chuỗi này có `clientWidth = 53` / `scrollWidth = 101` → clipped = true, viewer không đọc được bằng chứng xã hội. | Đổi nhãn ở `page.tsx:239` thành dạng không gây hiểu nhầm, ví dụ `sub={hasLearnerProgress ? \`TB ${avgCompletionPct}% · ${progressByUser.length} người học\` : 'trong cây'}`. Đổi `page.tsx:236` `grid-cols-3` → `grid-cols-1 sm:grid-cols-3` (hoặc bỏ `truncate` ở `stat-chip.tsx:27` và cho `sub` xuống dòng).
A5 | SAI | application/src/app/share/[slug]/page.tsx:267 (ShareTree không có resource) · application/src/components/learn/resources-section.tsx:55-64 | Đặc tả liệt kê "danh sách resource của từng node (xem)" trong khối `/share/[workspace-slug]`. Trang `/share/[slug]` **không render resource nào**. Ở trang node thì chạy đúng — đo ẩn danh trên `/share/sample-public-roadmap/n/sample-course`: "Thư viện tài liệu · 1 tài liệu được chia sẻ · Link (1) · AUDIT-E res root · https://example.com/root" (bản rà cũ ghi `node_resources count=0` nên chưa chứng minh được; nay `psql` có 5 dòng nên đã chứng minh). | Ở `full-tree.ts` thêm một truy vấn gộp `select node_id, count(*) from node_resources where workspace_id = ? group by node_id` rồi gắn `resourceCount` vào `ShareTreeNode`; ở `share-tree.tsx:184-194` thêm badge "N tài liệu" cạnh badge "N mục" (và cho hàng lá bung danh sách link khi mở rộng). Một round-trip, không N+1.
A6 | ĐỦ | application/src/app/share/[slug]/page.tsx:174-178, :243-255 | Đo trên server ẩn danh (không bypass): HTML chứa `<p class="text-xs text-muted-foreground">Người tạo đã hoàn thành <!-- -->84<!-- -->%</p>` + thanh progress. Bản rà cũ ghi THIẾU ("userId=null mọi query") → đã vá. | Không cần vá. (Chỉ hiện khi owner có dòng `user_node_progress` — đúng tinh thần "demo", không bịa số.)
A7 | ĐỦ | application/src/app/share/[slug]/page.tsx:143-159, :182-188 | Đo ẩn danh thật: `grep -c "Theo dõi roadmap"` = 0 (FollowButton ẩn khi `viewer === null`), không có UI trạng thái node của cá nhân, `ForkButton` render dạng link `/sign-in?next=/share/devops-test`. Không có truy vấn progress theo viewer. | Không cần vá.
A8 | ĐỦ | application/src/app/share/[slug]/n/[nodeSlug]/page.tsx:93-229 | `curl` ẩn danh `/share/devops-test/n/aws-account-hygiene-XS-w1-s0` → 200, 152623b. Có breadcrumb 4 cấp, header node, "Lộ trình bên trong (2)", Thư viện tài liệu, Journal, Bình luận, CTA đăng nhập. Slug sai → 404. | Không cần vá về nội dung (xem A13 về gate riêng tư, và mục UI/UX về CTA).
A9 | ĐỦ | application/src/app/share/[slug]/page.tsx:225-230 và :283-300 · application/src/components/share/fork-button.tsx:43-52 | `grep -o "Fork roadmap này" \| wc -l` trên HTML ẩn danh = **2** (một ở thanh trên, một ở khối CTA cuối trang), kèm dòng "Miễn phí · Không cần credit card · Fork xong là học được ngay". | Không cần vá ở trang share (trang node thiếu CTA fork — ghi ở mục UI/UX).
A10 | ĐỦ | application/src/app/api/og/route.tsx:39-130 · application/src/app/share/[slug]/page.tsx:87-105 | `/api/og?slug=devops-test` → **200 image/png 94714b**; biến thể node `&node=phase-1-foundational-track` → **200 image/png 97495b**; slug không tồn tại → **404**. Thẻ meta đủ: `og:title`, `og:description`, `og:image` (1200×630 + alt), `twitter:card=summary_large_image`. `/sitemap.xml` 200 và chỉ liệt kê workspace public (`sitemap.ts:48-51`), `/robots.txt` 200 với `Disallow: /w/`. | Không cần vá về hình (xem A13 về việc OG không kiểm visibility).
A11 | SAI | application/src/app/share/[slug]/n/[nodeSlug]/page.tsx:220 · application/src/lib/workspace.ts:54,59 · application/src/lib/rbac/resolve.ts:55,60 | Nhánh từ trang share ĐÚNG: `/sign-in?next=/share/devops-test`, và `sign-in/page.tsx:24-25` sanitize `next` (phải bắt đầu `/`, không `//`) rồi đẩy vào `/auth/callback?next=…` (`auth/callback/route.ts:16,30`). Nhánh từ trang NODE thì hỏng: CTA trỏ `/sign-in?next=/w/devops-test/n/<node>`; đo với người-lạ-đã-đăng-nhập `GET /w/devops-test` → **500** (không phải 404), và HTML trả về nhúng nguyên chuỗi `"message":"WORKSPACE_NOT_FOUND_OR_FORBIDDEN"` kèm stack có đường dẫn tuyệt đối của server. Nghĩa là viewer bấm "Đăng nhập để học" xong sẽ rơi vào trang lỗi. (`/w/khong-ton-tai` cũng 500 → không-tìm-thấy và không-đủ-quyền vẫn KHÔNG phân biệt được, phần đó vẫn đúng luật; sai ở chỗ mã trạng thái và trải nghiệm.) Hành vi ở bản production chưa kiểm chứng được (chỉ chạy được `next dev`). | (a) Đổi `n/[nodeSlug]/page.tsx:220` thành `next=/share/${slug}` (viewer chưa là member thì không có cửa vào `/w`); muốn giữ ý định fork thì thêm `&fork=1` và cho `share/[slug]/page.tsx` tự mở dialog fork khi thấy tham số. (b) Đổi `workspace.ts:54,59` và `resolve.ts:55,60` từ `throw new Error('WORKSPACE_NOT_FOUND_OR_FORBIDDEN')` sang `notFound()` của `next/navigation` → 404 dùng chung `src/app/not-found.tsx`, vẫn giữ tính không-phân-biệt-được, hết 500 và hết lộ stack.
A12 | ĐỦ | application/src/app/share/[slug]/page.tsx:211-217 · .../n/[nodeSlug]/page.tsx:128-134 · application/src/app/not-found.tsx:42-61 | Mọi bề mặt public đều có lối ra: trang share có "Trang chủ", trang node có "Quay về roadmap" (`grep -c` = 1), 404 có 3 nút (Trang chủ / Khám phá / Xem demo). `/share/khong-ton-tai` → 404 đúng trang đẹp. | Không cần vá (trừ hardcode slug ở not-found — xem mục UI/UX).
A13 | SAI (P0) | application/src/app/share/[slug]/n/[nodeSlug]/page.tsx:99-109 (thiếu hẳn kiểm `ws.visibility`) · application/src/app/api/og/route.tsx:48-57 ("Read-only lookup — no auth") | Rò rỉ workspace PRIVATE, đo hai lần độc lập: (1) **ẩn danh** `GET /share/sample-public-roadmap-0000-7n1w/n/sample-course` → **200, 137992b**, `<title>Sample Course · Sample Public Roadmap (Fork) · Roadmap</title>`; cùng lúc `GET /share/sample-public-roadmap-0000-7n1w` → **404**. (2) **người lạ đã đăng nhập** cùng URL node → **200, 137949b**; node thứ hai `phase-1-start` cũng 200. (3) `GET /api/og?slug=sample-public-roadmap-0000-7n1w` → **200 image/png 100136b** (slug không tồn tại thì 404) → tên workspace private lọt ra dưới dạng ảnh. `psql`: workspace này `visibility = private`. Gate ở `share/[slug]/page.tsx:138-141` (commit d2af20a) chỉ vá trang chỉ mục, hai bề mặt còn lại chưa từng được rà. | Đưa gate thành MỘT hàm dùng chung trong `application/src/lib/rbac/resolve.ts` (đúng luật "mọi truy cập workspace qua resolve.ts"), ví dụ `resolvePublicWorkspace(slug, viewerId): Promise<ResolvedWorkspace \| null>` gói cả `isViewerAllowed` hiện đang nằm lạc ở `share/[slug]/page.tsx:109-123`. Gọi ở: `share/[slug]/page.tsx:138`, `share/[slug]/n/[nodeSlug]/page.tsx:105` **và** `generateMetadata` dòng 48 (không thì title vẫn lộ), `api/og/route.tsx:57` (private → 404 y hệt không tồn tại). Kèm test: 3 URL private × (ẩn danh, người lạ, member, owner).
---
UI/UX & FE:
- application/src/app/share/[slug]/page.tsx:236 · application/src/components/learn/stat-chip.tsx:27 | `grid grid-cols-3` không có breakpoint. Đo Playwright @360px: 3 chip mỗi cái rộng 101px, dòng `sub` của chip thứ 3 bị cắt (`clientWidth 53` / `scrollWidth 101`) → con số bằng chứng xã hội "84% người hoàn thành" biến mất đúng trên thiết bị mà share link hay được mở nhất. | `grid-cols-1 sm:grid-cols-3`, hoặc bỏ `truncate` ở stat-chip.tsx:27 và cho `sub` `text-wrap`.
- application/src/app/share/[slug]/page.tsx:210 và :218 | Thanh trên cùng `justify-between` không có `flex-wrap`. Đo @360px: cụm bên phải rộng 287px + link "Trang chủ" 53px > container 328px → hàng phình cao 80px, chữ dồn cục. | Thêm `flex-wrap gap-y-2` ở dòng 210 và `flex-wrap justify-end` ở dòng 218; ở mobile ẩn bớt chip "👀 Read-only · N mục" (`hidden sm:inline-flex`).
- application/src/components/share/share-tree.tsx:203 | Mỗi cấp thụt thêm `ml-3 pl-4`; cây sâu 4 cấp. Đo @360px: link sâu nhất có `left = 292/360`, **15 phần tử** tràn khỏi viewport, `document.scrollWidth = 382 > clientWidth = 360` → cả trang bị cuộn ngang. @768px thì sạch (0 phần tử tràn). | Giảm thụt theo breakpoint (`ml-0 pl-2 sm:ml-3 sm:pl-4`) hoặc chặn thụt từ depth ≥ 3; thêm `break-words min-w-0` cho `<Link>` ở dòng 177-183.
- application/src/components/share/share-tree.tsx:151-160 | Nút bung/thu là ô 24×24 (đo thật). Đây là điều khiển DUY NHẤT của cây trên mobile, dưới ngưỡng chạm 44×44 khuyến nghị. | Giữ icon 16px nhưng nâng vùng chạm: `size-6 sm:size-6` → `p-2 -m-1` hoặc `size-11 sm:size-6`.
- application/src/app/share/[slug]/page.tsx:220 · application/src/components/learn/share-link-button.tsx:12 | Tiếng Việt lẫn tiếng Anh ngay cạnh nhau: "👀 Read-only · 166 mục", nút "Copy link share", badge "Roadmap · Read-only share" (dòng 257). | Thống nhất: "Chỉ xem · 166 mục", "Sao chép liên kết".
- application/src/app/(auth)/sign-in/page.tsx:74, :81-84, :116, :128, :133 | Đích đến của MỌI CTA trong Flow A là trang sign-in **100% tiếng Anh** ("Back", "Welcome back", "Sign in to continue your competency journey.", "Send magic link", "Continue with Google", "New here?…") trong khi toàn luồng trước đó tiếng Việt. Cú vấp đúng ngay điểm chuyển đổi. | Dịch toàn bộ trang sign-in sang tiếng Việt; giữ "Google" là tên riêng.
- application/src/app/(app)/w/[slug]/error.tsx:37, :39-42, :53 | Tiêu đề tiếng Anh "Couldn't load this workspace section" + nút "My workspaces" trộn với "Thử lại" tiếng Việt; và `{error.message}` in thẳng ra màn hình → đo được chuỗi kỹ thuật `WORKSPACE_NOT_FOUND_OR_FORBIDDEN` nằm trong HTML người dùng nhận. Đây là màn hình mà viewer Flow A rơi vào sau khi bấm "Đăng nhập để học" (xem A11). | Dịch tiêu đề/nút; thay `{error.message}` bằng câu người-đọc-được cố định, chỉ giữ `error.digest` để tra log.
- application/src/app/share/[slug]/ (không có `loading.tsx`) · application/src/app/discover/ (không có `loading.tsx`) | `find src/app -name loading.tsx` chỉ ra 4 file, TẤT CẢ nằm dưới `(app)/w/**`. Hai trang public — đúng những trang khách lạ gặp đầu tiên — không có skeleton. Đo: `/share/devops-test` 2.86s lần đầu (biên dịch), 0.18-0.37s khi ấm; `/discover` 0.08-0.10s. Trên mạng thật độ trễ này rơi vào khoảng trắng không phản hồi. | Thêm `src/app/share/[slug]/loading.tsx` (skeleton hero + 5 hàng cây) và `src/app/discover/loading.tsx` (lưới 6 thẻ xám), tái dùng khung của `(app)/w/[slug]/loading.tsx`.
- application/src/app/share/ và application/src/app/discover/ (không có `error.tsx`) | Chỉ có `(app)/error.tsx` và `(app)/w/[slug]/error.tsx` — cả hai KHÔNG bao các route public. Bằng chứng cùng loại: server cổng 3000 gặp lỗi chunk → `/share/devops-test` trả **500** với trang lỗi trần của Next, không có lối quay ra. | Thêm `src/app/share/error.tsx` + `src/app/discover/error.tsx`: thông điệp tiếng Việt + nút "Thử lại" + "Khám phá lộ trình khác".
- application/src/components/learn/vertical-roadmap.tsx:353-357 | Chú giải hardcode dữ liệu nghiệp vụ: "Cyan · Phase 1 / Purple · Phase 2 / Yellow · Phase 3 / Green · Phase 4 / Pink · Bonus". Đo trên `/share/sample-public-roadmap` (workspace chỉ có **2 node**, không có phase nào): chú giải vẫn in đủ 5 dòng → nói dối viewer. Vi phạm luật "cấm hardcode dữ liệu nghiệp vụ trong `src/components`". | Nhận `sections` (hoặc `treeRoots`) làm prop và sinh chú giải từ chính các node cấp 1 đang hiển thị; không có node thì không render `RoadmapLegend` (`share/[slug]/page.tsx:269`).
- application/src/app/share/[slug]/page.tsx:237-238 | Nhãn "Giai đoạn / cấp 1" và "Tuần / Buổi / cấp 2" là từ vựng riêng của lộ trình DevOps 12 tháng, hardcode trong `src/app`. Đo trên `/share/sample-public-roadmap`: hiện "0 / TUẦN / BUỔI / cấp 2" — vô nghĩa với workspace đó. | Lấy nhãn từ `nodeTypeLabel()` (`src/lib/tree/node-meta.ts`, đã có sẵn và ShareTree đang dùng) theo `nodeType` thật của cấp tương ứng; rỗng thì ẩn chip.
- application/src/app/not-found.tsx:56 | Trang 404 toàn cục hardcode `/share/devops-test` làm nút "Xem demo". Nếu workspace đó bị xoá hoặc chuyển private thì 404 dẫn sang… 404. Cùng lỗi hardcode nghiệp vụ trong `src/app`. | Đã có tiền lệ đúng ở `src/components/landing/hero.tsx:20` (`featured ? /share/${featured.slug} : '/discover'`) — dùng lại cách đó, hoặc bỏ nút này chỉ giữ "Khám phá".
- application/src/components/learn/resources-section.tsx:96-99 · application/src/components/learn/journal-section.tsx:77-80 | Ở chế độ `readOnly` (trang share công khai) phụ đề vẫn mời hành động không tồn tại: "Chưa có tài liệu — **hãy đóng góp** link / video / sách…" và "Chưa có bài nào — **viết ghi chú đầu tiên** cho mục này." Đo trên trang node ẩn danh: cả hai chuỗi đều xuất hiện dù không có nút nào. (Riêng `EmptyState` bên dưới đã phân nhánh `readOnly` đúng — chỉ dòng phụ đề quên.) | Cho phụ đề rẽ nhánh theo `readOnly` giống `EmptyState` ở dòng 106-116 / 87-97.
- application/src/components/learn/journal-section.tsx:113-114 · application/src/components/social/comment-thread.tsx:105-106 | Trang công khai hiển thị 8 ký tự đầu UUID người dùng làm "tên tác giả" và nhét **full UUID** vào thuộc tính `title=`. Vừa là bằng chứng xã hội tệ (viewer thấy "a1b2c3d4"), vừa lộ định danh nội bộ. Đã có sẵn `getPublicUserDisplay()` (`src/lib/auth/user-display.ts:103`, đợt 8 chưa commit) nhưng hai chỗ này không dùng. **Chưa dựng lại được bằng dữ liệu thật**: `select count(*) from node_journal_entries` = 0 và chưa có bình luận nào — kết luận đọc từ code. | Dùng `getPublicUserDisplay(authorUserId)` như `src/app/cert/[id]/page.tsx:73` đã làm; bỏ `title={authorUserId}`.
- application/src/app/share/[slug]/page.tsx:259-267 · application/src/components/share/share-tree.tsx:77 | Workspace 0 node: `ShareTree` `return null`, trang vẫn in "01 — Lộ trình chi tiết / 0 giai đoạn" rồi tới chú giải và CTA — một khoảng trống không giải thích. Trái `USER_FLOWS.md:565-568` ("hiện illustration + …, KHÔNG hiện placeholder data"). **Chưa kiểm chứng được bằng dữ liệu**: không có workspace 0 node trong DB lúc rà — kết luận đọc từ code. | Ở `page.tsx` kiểm `totalNodes === 0` → render `EmptyState` (`src/components/ui/empty-state.tsx` đã có) với câu "Lộ trình này chưa có bước nào" + nút "Khám phá lộ trình khác", và bỏ qua `NumberedSection` + `RoadmapLegend`.
- application/src/app/share/[slug]/n/[nodeSlug]/page.tsx:213-226 | Trang node **không có** CTA fork (`grep -c "Fork roadmap"` = 0), chỉ có "Đăng nhập để học". Viewer đọc sâu vào một bài rồi quyết định — đúng lúc đó lại không có nút fork. | Thêm `<ForkButton …/>` (đã là client component sẵn dùng) vào khối CTA cuối trang node, cạnh nút đăng nhập.
- application/src/components/discover/discover-grid.tsx:143-154 | Khi bộ lọc không khớp chỉ có một dòng chữ, không có nút gỡ lọc → người dùng phải tự nhớ mình đã lọc gì. | Thêm nút "Xoá bộ lọc" gọi `setQ(''); setDomain('all'); setSort('newest')`.
- application/src/components/discover/discover-grid.tsx:22-36, :160-206 | Thẻ discover nhận `ownerUserId` nhưng không dùng — không hiện tác giả, cũng không hiện mô tả với workspace nhiều root (xem A2). Front door mất hai tín hiệu tin cậy. | Hiện `getPublicUserDisplay(ownerUserId)` ở server (`discover/page.tsx`) rồi truyền xuống thẻ; kèm vá A2 để có mô tả.
---
ĐÃ VÁ TỪ BẢN RÀ CŨ:
- **A3 SAI → ĐỦ**: `getFullTree` + `ShareTree` (đợt 8, chưa commit) thay cho lối render 2 cấp. Đo: 166/166 node hiện trên một trang, sâu tới depth 4.
- **A6 THIẾU → ĐỦ**: tiến độ demo của creator đã có thật ("Người tạo đã hoàn thành 84%"), lấy từ một truy vấn gộp `user_node_progress` theo workspace.
- **A4 một phần**: bản cũ ghi "không có % hoàn thành"; nay % đã tính và render thật (84%). Vẫn để SAI vì nhãn mô tả sai bản chất con số và mobile cắt mất nó.
- **A5 một phần**: bản cũ không chứng minh được vì `node_resources` rỗng; nay đã đo được resource hiển thị đúng ở trang node. Vẫn để SAI vì `/share/[slug]` chưa liệt kê resource như đặc tả.
- Bản cũ ghi A2 "gate private đúng" — đúng, nhưng chỉ cho trang chỉ mục. Hai bề mặt anh em (`/share/[slug]/n/[nodeSlug]`, `/api/og`) chưa từng được rà và đang rò rỉ → mở bước mới A13.
- A11 bản cũ ghi ĐỦ; nay hạ xuống SAI vì đã đo được cú rơi 500 sau khi đăng nhập từ trang node.

CÒN LẠI (xếp theo mức nặng):
1. **P0 — Rò rỉ workspace private (A13).** `/share/[slug]/n/[nodeSlug]` và `/api/og` trả 200 cho workspace private với cả khách ẩn danh lẫn người lạ đã đăng nhập. Vá bằng một hàm gate dùng chung đặt trong `src/lib/rbac/resolve.ts`, gọi ở 4 điểm (page, generateMetadata của page node, share index, og route).
2. **P1 — Số đếm cây sai (A3b).** `getFullTree` cộng dồn descendantCount dựa trên một giả định thứ tự sai; viewer đang đọc "48 mục" thay vì 159, "15 mục" thay vì 46. Sửa `orderBy` (hoặc đếm đệ quy) VÀ chỉnh `EXPAND_ALL_LIMIT`/heuristic cùng lúc, kèm unit test đầu tiên cho `full-tree`.
3. **P1 — Dead-end sau đăng nhập (A11).** CTA trang node đẩy người mới vào `/w/<slug>` → 500 kèm stack. Đổi đích `next`, và đổi `throw new Error` ở `workspace.ts` + `resolve.ts` thành `notFound()`.
4. **P2 — Mobile của trang share (A4 + 3 mục UI/UX đầu).** Cuộn ngang 382/360, số social-proof bị cắt, thanh trên dồn cục, nút chạm 24px. Đây là bề mặt được mở trên điện thoại nhiều nhất.
5. **P2 — Mô tả lộ trình không bao giờ hiện (A2).** Cả hai workspace public đều nhiều root nên rơi về câu placeholder, kể cả trong `og:description` mà mạng xã hội đọc.
6. **P2 — Resource chưa lên trang share (A5).**
7. **P3 — Hardcode nghiệp vụ trong `src/components` / `src/app`**: chú giải 5 phase (`vertical-roadmap.tsx:353-357`, đang in sai trên workspace 2 node), nhãn "Giai đoạn/Tuần-Buổi" (`share/[slug]/page.tsx:237-238`), slug demo trong 404 (`not-found.tsx:56`).
8. **P3 — Thiếu `loading.tsx` và `error.tsx` cho `/share/**` + `/discover`**; thiếu empty-state khi workspace 0 node.
9. **P3 — Ngôn ngữ lẫn lộn** ở đúng điểm chuyển đổi: trang sign-in toàn tiếng Anh, "Read-only"/"Copy link share" trên trang share, error boundary nửa Anh nửa Việt in nguyên mã lỗi kỹ thuật.
10. **P3 — Danh tính người dùng trên bề mặt public**: journal/comment in UUID; `getPublicUserDisplay()` đã có nhưng chưa dùng (chưa kiểm chứng được bằng dữ liệu vì hai bảng đang rỗng).

Chưa kiểm chứng được:
- Hành vi bản **production** (`next build && next start`): cả phiên rà chỉ chạy được `next dev` — `.next` của server cổng 3000 đang hỏng chunk, và không dựng bản production để tránh làm đỏ gate của người khác. Các kết luận về mã trạng thái 500 và về việc Next che `error.message` ở production cần đo lại trên bản build thật.
- Rò rỉ journal/bình luận của thành viên ra trang share công khai (`journal-section.tsx:48-57` và `comment-thread.tsx` đọc thẳng DB không lọc theo tác giả, mà `node_journal_entries` **không có cột visibility** — đã `\d` xác nhận): hiện 0 dòng dữ liệu nên không dựng lại được. Cần seed rồi đo lại.
